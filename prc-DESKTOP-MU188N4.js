let bird = "Crow";
let isHungry = true;
let isThirsty = false;
if (isHungry) {
    console.log(`${bird} is hungry`);
    if (isThirsty) {
        console.log(`${bird} is Thirsty and need water`);
    }
}
;
export {};
